import { PageTransition } from "@/components/page-transition"

export function SamplingPage() {
  return (
    <PageTransition>
      <div>
        <h1 className="text-2xl font-bold mb-4">Sampling</h1>
        <p>This is the sampling page content.</p>
      </div>
    </PageTransition>
  )
}