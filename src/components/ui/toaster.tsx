"use client";

import {
  Toast,
  ToastClose,
  ToastDescription,
  ToastProvider,
  ToastTitle,
} from "@/components/ui/toast";

// Simple toast component for our demo
export function Toaster() {
  return (
    <ToastProvider>
      {/* We'll use a simpler implementation for this demo */}
    </ToastProvider>
  );
}